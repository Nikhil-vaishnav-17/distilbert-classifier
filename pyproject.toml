[project]
name = "distilbert-classifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.8.4",
    "gradio>=6.13.0",
    "ipykernel>=7.2.0",
    "jupyter>=1.1.1",
    "torch>=2.11.0",
    "transformers>=5.6.2",
]
